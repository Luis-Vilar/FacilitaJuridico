/*Apenas este endereço deve ser alterado para 
o URL do backend quando for para produçao*/
const BASE_URL = "http://localhost:3000";

// Endpoints para consumo da API
export const endpoints = {
  GET_BEST_ROUTE: `${BASE_URL}/addres`,
};
